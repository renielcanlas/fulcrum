# Claude Code adapter

Use the canonical instructions in [AGENTS.md](AGENTS.md). Read only the scoped `docs/` and `.ai/` context relevant to the task. Exchange durable artifacts using `.ai/handoffs/`; do not treat chat history as project truth.
