# Prompt and instruction registry

Prompts are versioned artifacts, reviewed like code, and referenced by `AgentExecution`. Keep provider-specific formatting in adapters; keep business rules and prohibitions in canonical docs and application policy.
