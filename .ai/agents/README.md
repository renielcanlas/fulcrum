# Agent registry

Each agent must have a versioned contract based on `contract-template.yaml`. Contracts define inputs, outputs, context, tools, permissions, prohibited actions, model requirements, evaluation, validation, fallback, and escalation. The orchestrator, not an agent, owns sequencing and human gates.
